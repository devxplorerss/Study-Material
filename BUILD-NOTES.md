To make the APK, install Android Studio/SDK and open this android/ folder. Replace the WebView URL in MainActivity.java with your deployed website URL, then Build > Generate Signed Bundle / APK > APK.

For local testing you can point the WebView at a local/test server, but production should use HTTPS.
